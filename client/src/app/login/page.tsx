"use client"
import React from 'react';
import LoginPage from '../../appComponents/Pages/pageLogin/pageLogin';

const PageLoginWrapper = () => {
  return(
    <div>
      <LoginPage/>
    </div>
  );
};

export default PageLoginWrapper;